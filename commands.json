{
    "mkdir": {
        "keywords": ["create", "make", "new", "folder", "directory", "mk", "mkdir"],
        "args": [
            {
                "type": "any",
                "required": true,
                "description": "Name of the folder"
            }
        ],
        "description": "Create a folder",
        "warning": false
    },
    "rmdir": {
        "keywords": ["delete", "remove", "folder", "directory", "rm", "rmdir"],
        "args": [
            {
                "type": "any",
                "required": true,
                "description": "Name of the folder"
            }
        ],
        "description": "Delete a folder",
        "warning": true
    },
    "exit": {
        "keywords": ["exit", "quit", "close", "shutdown", "stop", "bye"],
        "description": "Exit application",
        "warning": false
    }
}